creating a kafka topic

bin/kafka-topics.sh \
--create \
--topic cab_price_features \
--bootstrap-server localhost:9092 \
--partitions 1 \
--replication-factor 1

check - whether topic is created
bin/kafka-topics.sh --list --bootstrap-server localhost:9092

terminal 1
cd $KAFKA_HOME
bin/zookeeper-server-start.sh config/zookeeper.properties

terminal 2
cd $KAFKA_HOME
bin/kafka-server-start.sh config/server.properties

always start consumer first

terminal 3
sunbeam@Mohit92753DBDA:~/Downloads/bppr$ spark-submit --master local[*] --packages org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.7 kafka_consumer.py


terminal 4

sunbeam@Mohit92753DBDA:~/Downloads/bppr$ python3 kafka_consumer.py

